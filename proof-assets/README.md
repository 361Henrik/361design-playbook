# MagicPath proof assets

Deterministic, non-generative visual references built from the machine-readable contracts.

- `hostatlas-design-playbook.svg` belongs in the MagicPath project `HostAtlas Design Playbook`.
- `client-delivery.svg` belongs in the MagicPath project `Client Delivery`.

Do not merge these into one project. Do not upload the legacy checkerboard logo PNGs. Global MagicPath Assets is reserved for reusable raw media such as approved logos, icons, photography, video, and textures that are not themselves governance packages.
